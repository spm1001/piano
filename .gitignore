# Python / uv
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Editor / OS
.DS_Store

# Generated per-agent views — regenerated by `make sync`. The iron rule made
# visible: anything generated is NOT committed. (Distribution artefacts under
# plugins/ ARE committed — the org marketplace needs them as real files.)
.claude/
.agents/
.codex/
.mcp.json
mcp_config.json
# skills CLI lockfile IS committed (drift tracking), like a package-lock.

# cloudflared credentials — never commit (belong in ~/.cloudflared/)
cert.pem
*.cfargotunnel.json
**/.cloudflared/
